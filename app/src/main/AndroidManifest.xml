<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="workshop.g_s.myfitnessnutritiondairy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity android:name=".screens.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.fitness.fitness_activity.core.FitnessActivity"></activity>
        <activity android:name=".screens.nutrition.nutrition_activity.NutritionActivity"></activity>
        <!-- <activity android:name=".screens.draw.DrawActivity"> -->

        <activity android:name=".screens.camera.camera_activity.CameraActivity"></activity>
    </application>

</manifest>